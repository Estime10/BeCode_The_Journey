const LoginButton = ({onClick}) => { 
    return (
        <button  onClick={onClick} className="bg-blue-800 text-white rounded-md p-1 mr-1 w-24 text-base font-medium hover:bg-blue-900 focus:outline-none focus:ring-2 focus:ring-blue-800" >Login
        </button>
        )}
    export default LoginButton
