const PostButton = ({onClick}) => { 
    return (
        <button  onClick={onClick} className="bg-blue-800 text-sm mt-2 text-white rounded  px-3 py-1  font-medium hover:bg-blue-900 focus:outline-none focus:ring-2 focus:ring-blue-800" >Post
        </button>
        )}
    export default PostButton
